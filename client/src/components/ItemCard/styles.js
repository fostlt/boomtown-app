import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    card: {
      marginTop: "2rem",
      width: 500,
    },
    media: {
    height: 250,
    },
    profile: {
      fontSize: "1.3rem",
    },
    button: {
      border: "1px solid rgba(0, 0, 0, 0.50)"
    },
    title: {
      fontSize: "2rem"
    },
    created: {
      fontSize: "1.3rem",
      color: "#909090"
    },
    description: {
      fontSize: "1.2rem",
    },

  });
export default styles;
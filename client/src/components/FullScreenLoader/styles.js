import {createStyles} from "@material-ui/styles";
const styles = () =>
  createStyles({
    container: {
      marginTop: "40vh",
    },
    text: {
      fontSize: "2rem",
    },

  });
export default styles;